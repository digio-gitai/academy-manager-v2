# Academy-Manager — AI 어시스턴트용 컨텍스트

새 대화창을 열고 이 폴더를 연결했다면, 설명 다시 안 들어도 되도록 여기부터 읽으세요.
(2026-07-22 기준, 코드 직접 확인해서 검증한 내용 — Claude.ai 프로젝트 메모리보다 이 파일이 최신입니다.)

## 프로젝트

Johny(정재훈)가 압구정에서 운영할 수학 학원(사과나무 학원)을 위한 Streamlit 학원관리 앱.
장기적으로는 예비창업패키지(정부 창업지원금) 지원을 목표로 SaaS화도 고려 중.

## 사용자 성향

- 비개발자. 코드는 Claude가 전부 작성.
- 존댓말, 완전 초보 기준 설명, 한 번에 하나씩 확인받고 진행.
- CMD 사용 (PowerShell 아님). `cd`로 드라이브 이동 시 `/d` 옵션 필요 (`cd /d "G:\..."`).
- git 커밋은 보통 사용자가 직접 CMD에서 실행. `.git\index.lock`이 종종 남는 문제가 있음 —
  파일 탐색기에서 "숨김 항목 보기" 켜고 `.git\index.lock` 직접 삭제해야 풀림.

## 실제 현재 상태 (2026-07-22 코드 확인 결과)

- **DB: Supabase(PostgreSQL) 완료.** `db_connect.py`가 psycopg2로 연결, `database.py`가 이를 통해 동작.
  SQLite 아님. `wake_db.py`는 무료 플랜 슬립(7일 비활성 시 일시정지) 대비용 — "마이그레이션"이 아니라 "깨우기" 스크립트.
- **알림: 카카오 → SMS 전환 완료.** 사업자등록이 없어 카카오 친구톡/알림톡 불가 → `sms_sender.py`로 솔라피 SMS 발송
  (링크 단축, 일괄 발송 포함). 카카오 관련 코드는 더 이상 진행 안 함.
- **브랜드명 확정.** `branding.py`: 학원명 "사과나무 학원", 강사 "정재훈". Math Management는 내부 앱 타이틀로만 남음.
- **past_exam_analyzer 5페이지 구조 정상.** `_build_page3b` 죽은 코드는 제거함 (커밋 7b69d50).
- **문제은행(question_bank)은 사실상 비어있고, 유사문제 매칭도 미완성.**
  자세한 진단과 개선 계획은 **`PLAN_문제은행_유사문제매칭.md`** 참고 — 이 기능 얘기 나오면 그 파일부터 읽을 것.
- README.md는 오래된 템플릿 + git merge conflict 마커가 그대로 남아있어 내용이 실제와 안 맞음 (정리 필요, 아직 안 함).

## 기술 스택

Python + Streamlit + Supabase(PostgreSQL) + Claude API(보고서) + GPT-4o(OCR 분석) + Google Vision OCR + 솔라피 SMS.
PDF는 Playwright/WeasyPrint, 수식은 LaTeX/SVG 렌더링 (HWP 변환 아님 — 편집 가능한 한글 수식 개체로 만드는 건
현재 기술로도 리스크가 커서 이 프로젝트에서는 채택 안 함).

## 남은 계획 (우선순위 순)

1. 문제은행 유사문제 매칭 — `PLAN_문제은행_유사문제매칭.md` 참고 (단원 태그 고정 → DB 채우기 → 임베딩 유사도 → Claude 재검증)
2. abc 과제관리 시스템 (장기)
3. 예비창업패키지 지원 (내년 1~2월)
4. React 리빌드 (장기, 배포 방식 전면 변경 예정)
